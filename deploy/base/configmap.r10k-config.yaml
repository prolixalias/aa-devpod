# configmap.r10k-config.yaml
---
apiVersion: v1
kind: ConfigMap
metadata:
  namespace: devcontainer
  name: r10k-config
data:
  r10k.yaml: |
    ---
    :cachedir: '/var/cache/r10k'
    :sources:
      :base:
        remote: 'git@github.com:fervidus/control-repo_fervid.git'
        basedir: '/etc/puppetlabs/code/environments'
