# configmap.user-gitconfig.yaml
---
apiVersion: v1
kind: ConfigMap
metadata:
  namespace: devpod
  name: user-gitconfig
data:
  .gitconfig: |-
    [user]
      name = Romil Surti
      email = romil.surti@aa.com
    [push]
      default = simple
    [pull]
      rebase = true